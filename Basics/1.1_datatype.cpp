#include<iostream>
using namespace std;

int main()
{
    //int, long , long long

    //float , double -> can also store int value.

    // String and getline
    // string s;
    // cin>>s;// will only store the char before the space
    // cout<<s;// will print hey
    // to store pranshu u need to have another variable to store and then  print it.

    // it will store whole hey pranshu
    // string s1, s2;
    // cin>>s1>>s2;
    // cout<<s1<<" "<<s2;

    // use getline to store multiple words easily
    // string str;
    // getline(cin,str);//internal fn to store multiple words in same var including spaces but it will not pickup next line after enter(line break).
    // cout<<str;

    // Character , can also be stored using string datatype but it takes more size.
    // char ch;// char ch = 'h';
    // cin >> ch;
    // cout << ch;


    // Everything takes memory so according to need we use the desired data type.
    
    return 0;
}